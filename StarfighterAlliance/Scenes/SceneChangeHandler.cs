namespace StarfighterAlliance.Scenes;

/// <summary>
///     Represents the method that will handle a scene change event.
/// </summary>
/// <param name="args">The arguments containing context for the scene change, including the old scene controller.</param>
public delegate void SceneChangeHandler(SceneChangeArgs args);